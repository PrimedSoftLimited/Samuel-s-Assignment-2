<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- For text views-->

    <style name="textLook" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">#FFFFFF</item>
        <item name="android:drawablePadding">16dp</item>
        <item name="android:paddingBottom">8dp</item>
    </style>

    <!-- For the image view-->

    <style name="imageLook" parent="Base.Widget.AppCompat.AutoCompleteTextView">
        <item name="android:drawableTop">@drawable/camera</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>

    </style>

    <style name="bottomText" parent="Base.Widget.AppCompat.AutoCompleteTextView">
        <item name="android:textColor">#616161</item>
    </style>


</resources>